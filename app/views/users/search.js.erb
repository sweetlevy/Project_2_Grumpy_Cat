var formHTML = $("<%= j(render 'boards/search') %>");
var modal = $('<div class="modal">');
var modalContent = $('<div class="modal-content">');
var container = $('#container');
modal.empty();
modalContent.empty();
modalContent.append(formHTML);
modal.append(modalContent);
container.append(modal);
showModal();
